name: br_utils
description: Collection of Widgets and helpful Methods that every developer needs.
version: 6.1.3
#homepage: https://paulorlima9.github.io
repository: https://github.com/paulorlima9/br_utils
issue_tracker: https://github.com/paulorlima9/br_utils/issues
funding:
  - https://www.github.com/sponsors/paulorlima9

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.2
  fluttertoast: ^8.2.4
  connectivity_plus: ^5.0.2
  http: ^1.1.0

dev_dependencies:
  lints: ^2.1.1

flutter:
  plugin:
    platforms:
      android:
        package: com.nb.br_utils
        pluginClass: NbUtilsPlugin
      ios:
        pluginClass: NbUtilsPlugin
      linux:
        pluginClass: br_utils_plugin
      macos:
        pluginClass: NbUtilsPlugin
      windows:
        pluginClass: NbUtilsPlugin
    
  fonts:
    - family: LineAwesomeIcons
      fonts:
        - asset: fonts/LineAwesome.ttf
          weight: 400

  uses-material-design: true
  assets:
    - assets/icons/
    - assets/lottie/

topics:
  - widgets
  - extensions
  - utils
  - chatgpt